publish:
  image: mcr.microsoft.com/dotnet/sdk:8.0
  stage: publish
  needs: 
    - job: build
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
  script: 
    - dotnet nuget push -k $NUGET_KEY -s https://www.nuget.org "**/*.nupkg" --skip-duplicate